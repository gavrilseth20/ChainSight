[build]
builder = "NIXPACKS"
watchPatterns = ["Backend/**/*.py", "AI/**/*.py"]

[deploy]
startCommand = "cd Backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ALWAYS"
